# RMUL2027 语义路线图与代价地图语义掩膜实施计划（待审批）

## 1. 审批状态与边界

- [ ] 批准 A：语义路线图
- [ ] 批准 B：代价地图语义掩膜
- [ ] 批准为了 B 修改 `26Navigation-master` 中的 Nav2 参数、launch 和地图资源

未勾选并获得明确批准前，不实施本文件中的 A、B。现有 tunnel 行为树、世界坐标到 `map` 的变换和有序中心线控制继续作为当前可运行基线。

## 2. 坐标约定

路线配置统一保存 Gazebo/Blender 世界绝对坐标，禁止在同一配置中混入 Nav2 坐标。

RMUL2027 红方出生位姿为：

```text
world_T_map = (x=5.0, y=-3.0, yaw=π)
```

决策层加载路线后转换为 Nav2 `map`：

```text
p_map = R(-π) × (p_world - [5.0, -3.0])
yaw_map = normalize(yaw_world - π)
```

隧道世界坐标基线：

| 语义 | world 坐标 | map 坐标 | 正向 map yaw |
|---|---:|---:|---:|
| 红方出生/补给节点 | `(5.0,-3.0)` | `(0.0,0.0)` | — |
| 隧道入口准备节点 | `(4.0,-3.4)` | `(1.0,0.4)` | `0` |
| 隧道中心节点 | `(2.5,-3.4)` | `(2.5,0.4)` | `0` |
| 隧道出口节点 | `(0.5,-3.4)` | `(4.5,0.4)` | `0` |

反向通行自动反转点序，map yaw 为 `π`，对应 world yaw 为 `0`。

## 3. A：语义路线图

### 3.1 数据模型

在 `26Decision-debug/rm_behavior_tree/config/` 增加 `RMUL2027_route_graph.yaml`：

```yaml
frame:
  source: world
  map_origin: [5.0, -3.0, 3.141592653589793]
nodes:
  red_supply: {pose: [5.0, -3.0], region: red_half}
  tunnel_red_entry: {pose: [4.0, -3.4], region: tunnel_approach}
  tunnel_center: {pose: [2.5, -3.4], region: tunnel}
  tunnel_far_exit: {pose: [0.5, -3.4], region: center_side}
  center: {pose: <现场标定>, region: center}
edges:
  - id: red_main_road
    type: normal
    bidirectional: true
    from: red_supply
    to: center
  - id: red_boundary_tunnel
    type: tunnel
    bidirectional: true
    from: tunnel_red_entry
    via: [tunnel_center]
    to: tunnel_far_exit
    controller: TunnelController
```

正式实施时补全普通道路节点，不在代码里散落坐标常量。加载时检查节点引用、重复 ID、有限数值、点序和坐标系字段；配置错误时禁止启动 tunnel 任务并输出明确日志。

### 3.2 决策接口

新增 `SelectSemanticRoute` 行为树节点，输入：当前位置、任务目标、联盟方、是否强制隧道；输出：

- 选择的 edge ID；
- 入口准备位姿；
- 有序中心线路径；
- 失败回撤路径；
- 路线所需 controller 和语义掩膜模式。

选择顺序：

1. 在任何 `ComputePathToPose`/`NavigateToPose` 前选择语义边。
2. tunnel 任务强制选择 tunnel edge，不参与栅格最短路竞争。
3. 根据机器人到两个端点的距离确定方向；距离接近时结合任务目标所在区域消除抖动。
4. 一次任务选定后锁定 edge 和方向，直至完整离开出口或回撤到入口。

### 3.3 状态机

```text
ROUTE_SELECTED
  → APPROACH_ENTRY
  → ALIGN_AT_ENTRY
  → COMMITTED
  → TRAVERSING
  → EXIT_CONFIRMED
  → NORMAL_NAVIGATION

TRAVERSING failure
  → RETREATING_ON_CENTERLINE
  → APPROACH_ENTRY
```

- `ALIGN_AT_ENTRY`：位置约 `0.06 m`、yaw 容差 `0.10～0.20 rad`。
- `COMMITTED` 后不再调用全局规划器、不重新选择普通道路。
- `EXIT_CONFIRMED` 使用出口后方距离/区域判定，而不只依赖最后一个控制周期。
- 隧道内直接调用 `/follow_path`，因此不进入带 Spin 的 `bt_navigator` recovery tree。

### 3.4 测试与验收

- 单元测试：world→map 转换、角度归一化、双向端点选择、点序、配置错误。
- 行为树测试：隧道选择发生在首次全局路径请求之前；承诺后 edge 不改变。
- 故障测试：中点失败后保持姿态沿中心线退回入口。
- 仿真验收：正向、反向、低血量返程各连续 5 次，无大路绕行、无隧道内 Spin。

## 4. B：代价地图语义掩膜

### 4.1 掩膜资源

以 `RMUL2027.pgm/yaml` 的尺寸、分辨率、origin 为唯一模板生成两套独立掩膜：

1. `RMUL2027_tunnel_preference_mask`：隧道中心走廊低值，大路高代价，但不把必要逃生区域设为 lethal。
2. `RMUL2027_tunnel_speed_mask`：入口缓速区和隧道限速区使用绝对速度编码。

生成脚本必须从世界坐标经同一变换投影到栅格，禁止手工目测平移。输出后叠加到静态地图做像素级检查。

### 4.2 Nav2 组件

新增 lifecycle 节点：

- preferred-lane mask map server；
- type `0` 的 costmap filter info server；
- speed mask map server；
- type `2`（绝对 m/s）的 costmap filter info server。

Costmap 配置：

- `KeepoutFilter` 同时加入 global/local costmap 的 `filters`，使全局规划和局部执行语义一致；
- `SpeedFilter` 只加入 local costmap，避免 global/local 同时发布速度限制导致边界抖动；
- 保留原 inflation 参数，不通过全场缩小 inflation 来适配隧道；
- mask、filter info 使用独立 topic，避免类型或 QoS 串线。

### 4.3 任务级启停

决策层在 `ROUTE_SELECTED`、首次全局路径生成前启用 tunnel preference；进入普通任务后关闭。启停采用具备确认响应的参数服务/专用服务，不使用无确认的普通 topic。

安全规则：

- 启用失败：不得宣称掩膜已生效；仍可依靠当前已提交中心线路径完成隧道任务。
- 关闭失败：暂停普通路线生成，避免残留的大路高代价影响比赛策略。
- 每次切换记录任务 ID、mask 模式和参数响应。

### 4.4 测试与验收

- 校验两个 mask 与主地图 width、height、resolution、origin 完全相同。
- 查询 filter info、mask topic 和 lifecycle active 状态。
- tunnel 模式：入口全局路径偏向隧道，中心线保持低代价，隧道区速度受限。
- normal 模式：掩膜关闭，普通道路代价恢复。
- 动态障碍仍由 obstacle/STVL layer 处理，语义掩膜不得覆盖真实 lethal obstacle。
- 正反向各运行 5 次，记录全局路径、local costmap、速度限制和控制误差。

## 5. 预计改动文件

审批 A 后，仅修改/新增 `26Decision-debug`：

- 路线图 YAML、加载器、选择节点、状态条件节点；
- tunnel 行为树和 CMake；
- 单元/集成测试与 README。

审批 B 且扩展修改范围后，预计修改 `26Navigation-master`：

- `rm_nav_bringup/config/simulation/nav2_params_sim.yaml`；
- `rm_nav_bringup/launch/bringup_sim.launch.py`；
- `rm_nav_bringup/map/` 下的 mask PGM/YAML；
- 实车配置在仿真验收通过后另行审批，不自动同步。

## 6. 审批后实施顺序

1. 先完成 A 和自动测试。
2. 标定普通道路节点及隧道出口确认区域。
3. 生成并可视化检查 B 的掩膜。
4. 接入 lifecycle/filter 配置和任务级启停。
5. 完成正反向、返程和失败回撤仿真验收。
6. 提交验收记录后，再决定是否迁移到实车配置。
