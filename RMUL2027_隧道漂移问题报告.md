# RMUL2027 隧道漂移问题报告

## 1. 结论

截图中的偏移不能靠修改 `robot_radius` 修复。`robot_radius` 只参与碰撞检查；路径/点云相对地图漂移的直接检查对象应是运行版本、TF、仿真时间、FAST-LIO 里程计和 slam_toolbox 的 `map→odom` 校正。

补充事实：出现漂移时只启动了导航，没有启动决策。因此决策行为树、`SelectTunnelDirection`、`FollowTunnelPath` 以及 tunnel waypoint 不可能是这次漂移的成因。此前将截图中的多条路径优先关联到旧版决策流程，是把图像形态当成了运行证据，判断不成立。

当前存在两个可确认问题：

1. 仿真 launch 默认 `use_sim_time=True`，但 `nav2_params_sim.yaml` 中 Nav2 多个节点硬编码为 `use_sim_time: False`。这会让 Gazebo 传感器/TF 与 Nav2 使用不同时间基准，可能造成 TF 查询错时、局部代价残影及控制路径相对机器人跳动。
2. 截图中同时出现多条紫/白线，但在没有决策节点的前提下，它们只能来自 Nav2/RViz 自身（例如 RViz 历史显示、planner/controller 发布的不同 path topic，或人工目标），不能归因于 tunnel 决策。需要先识别每个 RViz display 的具体 topic 和 history/decay 参数。

上一次自动验收还发现独立阻塞：Livox Gazebo 插件在发布点云时触发 `gazebo::transport::Connection` 空指针断言，`gzserver` 退出，继而无法建立 `map→base_link`。因此尚没有证据证明新 tunnel 控制代码已完成一次动态运行。

## 2. 截图现象

- 紫色和白色路径不是单一隧道中心线，而是向不同远端发散并构成三角形。
- 青色障碍/膨胀区域沿多条方向形成宽带；若这些是同一墙体在不同时间的观测，说明 TF 或定位随时间变化后旧观测没有及时清除。
- 机器人没有稳定贴合一条直线目标轨迹。
- 仅凭一张 RViz 截图无法区分“真实位姿漂移”和“RViz 同时保留多次规划结果”，必须记录 topic 与 TF 才能定责。

## 3. 根因优先级

### 已排除：决策层导致漂移

漂移发生时决策未启动，所以旧/新 tunnel 行为树都不在因果链中。即使 RViz 中路径形态类似旧 waypoint 路线，也不能据此推断旧决策进程存在。

### P0：仿真时间源不统一

`bringup_sim.launch.py` 默认使用 Gazebo `/clock`，但 Nav2 仿真 YAML 将 `bt_navigator`、controller、local/global costmap、planner、behavior server 等设为 `False`。

修复：将仿真配置中的全部 `use_sim_time` 统一为 `True`，启动后逐个检查：

```bash
ros2 param get /controller_server use_sim_time
ros2 param get /local_costmap/local_costmap use_sim_time
ros2 param get /global_costmap/global_costmap use_sim_time
ros2 param get /slam_toolbox use_sim_time
```

四者必须一致，并检查 `/scan`、`/Odometry`、TF 时间戳随 `/clock` 单调增长。

### P1：定位与里程计 TF 跳变

期望唯一链路：

```text
map --(slam_toolbox)--> odom --(FAST-LIO)--> base_link
```

检查是否有两个节点同时发布同一变换；以 20～50 Hz 采集 `map→odom`、`odom→base_link` 和 Gazebo ground truth。若机器人静止时 yaw 或平移持续变化，先修 LIO/scan matching，不调路径控制器。

### P2：RViz 同时显示多个导航 Path topic 或保留历史消息

验证：

```bash
ros2 topic list | grep -E 'plan|path'
ros2 topic info -v /plan
```

逐个核对截图中白色、紫色 display 订阅的话题，清空 display 或重启 RViz 后观察。多条线本身是显示/规划现象，不等同于机器人位姿漂移；真正漂移应以 TF 对 ground truth 的误差判定。

### P3：隧道环境导致 scan matching 退化

长直、重复边界对沿隧道方向的约束较弱；高速运动时更明显。当前普通 `FollowPath` 最大 x/y 速度均为 `5.0 m/s`，进入准备点前可能给 LIO 和 slam_toolbox 造成过大帧间位移。

修复顺序：

1. 出生后静止完成 IMU/LIO 初始化，再放行导航。
2. 将入口接近速度限制到约 `0.5～1.0 m/s`，隧道维持现有 `0.30 m/s`。
3. 核对 Livox 点字段时间单位、IMU 与雷达时间差和仿真外参。
4. 记录 scan matcher response；低置信度时保持里程计预测，不接受大幅 `map→odom` 跳变。

另外，Livox Gazebo 插件原先把生成一帧点云时消耗的主机墙钟时间写入每个点的 `offset_time`。这些射线实际上在同一个 Gazebo 仿真时刻求值；主机负载越高，伪造的帧内时间跨度越大，FAST-LIO 会据此执行错误的运动畸变补偿。现已将仿真点的 `offset_time` 设为 0，并把 LaserScan 的 `scan_time` 从 `0.3333 s` 修正为与 10 Hz 传感器一致的 `0.1 s`。

动态验证时还发现 slam_toolbox 无法持续处理约 8.5 Hz 的 `/scan`，其 message-filter queue 周期性满。定位配置现将 `throttle_scans` 设为 2（约 4～5 Hz），并把声明的有效雷达范围从不匹配的 `0～20 m` 修正为实际 `/scan` 的 `0.45～10 m`，避免无效量程参与匹配和持续积压。

### P4：局部障碍残影

统一时间和 TF 后再检查 `/scan` clearing。确认 `raytrace_max_range` 不小于有效 marking 距离、局部 rolling costmap 始终以机器人为中心，并在重定位跳变后清空一次 local costmap。不要用持续清图掩盖定位错误。

## 4. 建议修复与验证流程

1. 修复 Livox 插件崩溃，保证 `/livox/lidar`、`/scan`、`/Odometry` 连续发布。
2. 统一所有仿真节点为 Gazebo 时间。
3. 清空 RViz 历史并辨认各条 Path 的来源 topic。
4. 静止采集 30 秒 TF，要求 `map→base_link` 无持续漂移或跳变。
5. 低速直行采集 Gazebo ground truth 与 `map→base_link`，分别计算平移/yaw 误差。
6. 只发送入口目标，验证入口位置和 yaw。
7. 直接观察一次 `/follow_path` 三点中心线，确认穿越期间没有新的全局 `/plan`。
8. 最后测试失败回撤和补血返程。

建议验收阈值：静止 30 秒平移漂移小于 `0.03 m`、yaw 漂移小于 `0.02 rad`；单次隧道穿越相对 ground truth 最大横向误差小于 `0.10 m`，入口 yaw 误差小于 `0.20 rad`，穿越期间全局规划请求数为 0。

## 5. 关于局部 `robot_radius=0`

仿真 local costmap 的 `robot_radius` 为 `0.30 m`，局部 `inflation_radius` 为 `0.0 m`。全项目导航配置中的 local/global costmap 与 TunnelController 圆形 footprint 已统一为 `0.30 m`；全局 `inflation_radius` 保持 `0.325 m`。局部膨胀关闭不会修复 TF/定位漂移，并且局部与全局安全余量不同，当前设置应作为 tunnel 调试参数单独验收。
